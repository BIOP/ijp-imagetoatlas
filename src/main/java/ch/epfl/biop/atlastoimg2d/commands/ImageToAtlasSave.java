package ch.epfl.biop.atlastoimg2d.commands;

import org.scijava.command.Command;
import org.scijava.plugin.Plugin;

@Plugin(type = Command.class, menuPath = "Plugins>BIOP>Image To Atlas>Save")
public class ImageToAtlasSave implements Command {
    @Override
    public void run() {

    }
}

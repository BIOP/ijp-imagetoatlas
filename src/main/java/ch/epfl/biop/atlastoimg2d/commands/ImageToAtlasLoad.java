package ch.epfl.biop.atlastoimg2d.commands;

import org.scijava.command.Command;
import org.scijava.plugin.Plugin;

@Plugin(type = Command.class, menuPath = "Plugins>BIOP>Atlas>Image To Atlas>Load")
public class ImageToAtlasLoad implements Command {
    @Override
    public void run() {

    }
}
